const $$=(s,r=document)=>[...r.querySelectorAll(s)]; const $=(s,r=document)=>r.querySelector(s);
const cards=$$('[data-gallery]'); const overlay=$('#galleryOverlay');
let index=0;
function show(i){ index=(i+cards.length)%cards.length; const c=cards[index]; $('#galleryDetailImage').src=c.dataset.image; $('#galleryDetailTitle').textContent=c.dataset.title; $('#galleryDetailCategory').textContent=c.dataset.category; $('#galleryDetailDescription').textContent=c.dataset.description; $('#galleryDetailYear').textContent=c.dataset.year; $('#galleryDetailRole').textContent=c.dataset.role; $('#galleryCurrent').textContent=String(index+1).padStart(2,'0'); $('#galleryTotal').textContent=String(cards.length).padStart(2,'0'); overlay.classList.add('open'); overlay.setAttribute('aria-hidden','false'); document.body.style.overflow='hidden'; }
function close(){overlay.classList.remove('open');overlay.setAttribute('aria-hidden','true');document.body.style.overflow='';}
cards.forEach((c,i)=>{c.addEventListener('click',()=>show(i));c.addEventListener('keydown',e=>{if(e.key==='Enter')show(i)});});
$('#galleryPrev')?.addEventListener('click',()=>show(index-1)); $('#galleryNext')?.addEventListener('click',()=>show(index+1)); overlay?.addEventListener('click',e=>{if(e.target===overlay)close()}); $$('[data-close]',overlay).forEach(b=>b.addEventListener('click',close)); document.addEventListener('keydown',e=>{if(e.key==='Escape')close();if(overlay?.classList.contains('open')&&e.key==='ArrowLeft')show(index-1);if(overlay?.classList.contains('open')&&e.key==='ArrowRight')show(index+1)});
